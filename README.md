Итоговая аттестация

# Currency Converter

**Автор:** Аделя Рашитова  
**Дата:** 6.05.2026

## Описание

Приложение для конвертации валют с использованием внешнего API и сохранением истории операций. Позволяет быстро и удобно конвертировать суммы между популярными валютами с актуальными курсами.

## Функциональность

* Конвертация между популярными валютами (USD, EUR, GBP, JPY, CNY, RUB, CAD, AUD, CHF).
* Получение актуальных курсов через API [exchangerate-api.com](https://www.exchangerate-api.com/).
* Кэширование курсов на 1 час для снижения числа запросов к API.
* Сохранение истории конвертаций в файл `history.json`.
* Отображение истории в табличном виде с возможностью очистки.
* Валидация ввода: проверка на число и положительность суммы.
* Интуитивно понятный графический интерфейс (GUI).

## Примеры тестов
Тест 1: Конвертация 100 USD в EUR.
Тест 2: Конвертация 500 RUB в USD.
Тест 3: Проверка обработки ошибки — ввод текста вместо числа.
Тест 4: Проверка обработки ошибки — отрицательная сумма.
Тест 5: Конвертация с одинаковыми валютами (результат = сумма).
Тест 6: Очистка истории и проверка, что таблица пуста.


## Установка и запуск

## Как использовать
1. Сохраните каждый блок кода в соответствующий файл в одной папке.
2. Настройте Git‑репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"

 ## Использование
Введите сумму для конвертации.
Выберите валюту «Из» и «В».
Нажмите кнопку «Конвертировать».
Результат отобразится в поле «Результат».
Все операции сохраняются в истории (таблица внизу).
Для очистки истории нажмите кнопку «Очистить историю».
Примеры тестов 

## Получение API‑ключа
Перейдите на сайт .
Зарегистрируйтесь (можно использовать бесплатную учётную запись).
После регистрации войдите в личный кабинет.
В разделе «API Keys» создайте новый ключ или используйте существующий.
Скопируйте API‑ключ (строка из букв и цифр).

### Требования

* Python 3.6 или выше.
* Библиотеки: `requests`, `tkinter` (обычно входит в поставку Python).

### Установка зависимостей

Откройте терминал и выполните команду:

```bash
pip install requests
